name: Build Analysis

on:
  push:
    branches:
      - main
    paths:
      - 'analysis/**'
      - '.github/workflows/build-analysis.yml'
  pull_request:
    paths:
      - 'analysis/**'
      - '.github/workflows/build-analysis.yml'

# Cancel in-progress runs on same PR
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Explicit permissions (principle of least privilege)
permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    defaults:
      run:
        working-directory: analysis

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Setup mise
        uses: jdx/mise-action@v2

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: wrapper
          cache-read-only: ${{ github.event_name == 'pull_request' }}

      - name: Run ktlint
        run: mise run lint-analysis
        working-directory: .

      - name: Determine version
        id: version
        run: |
          if [ "${{ github.ref_type }}" == "tag" ]; then
            echo "release=${{ github.ref_name }}" >> $GITHUB_OUTPUT
          else
            echo "release=${{ github.sha }}" >> $GITHUB_OUTPUT
          fi
        working-directory: .

      - name: Build and test
        run: mise run ci-build-analysis ${{ steps.version.outputs.release }}
        working-directory: .

      - name: Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: dependacharta-jar-${{ github.run_id }}
          path: analysis/build/libs/dependacharta.jar
          retention-days: 1
          if-no-files-found: error

      - name: Upload SBOM
        uses: actions/upload-artifact@v4
        with:
          name: analysis-sbom-${{ github.run_id }}
          path: analysis/build/reports/cyclonedx/bom.json
          retention-days: 1
          if-no-files-found: error

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: analysis-test-results-${{ github.run_id }}
          path: analysis/build/test-results/*/*.xml
          retention-days: 1
          if-no-files-found: warn

      - name: Upload coverage reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: analysis-coverage-${{ github.run_id }}
          path: analysis/build/reports/jacoco/test/*.xml
          retention-days: 1
          if-no-files-found: warn

      - name: Publish test results
        uses: dorny/test-reporter@v1
        if: always() && github.event.pull_request.head.repo.full_name == github.repository
        with:
          name: Analysis Unit Tests
          path: analysis/build/test-results/*/*.xml
          reporter: java-junit
          fail-on-error: false

  sonarcloud:
    name: SonarCloud Analysis
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    needs: build
    # SonarCloud project is configured for the upstream MaibornWolff repo only.
    # The Futured fork has no SONAR_TOKEN and never will, so skip the job there.
    if: |
      github.repository == 'MaibornWolff/DependaCharta' &&
      (github.event.pull_request.head.repo.full_name == github.repository || github.event_name != 'pull_request')

    defaults:
      run:
        working-directory: analysis

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0  # Shallow clones should be disabled for better analysis relevancy

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Setup mise
        uses: jdx/mise-action@v2

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: wrapper
          cache-read-only: true

      - name: Build project (for bytecode analysis)
        run: mise run ci-compile-analysis
        working-directory: .

      - name: Download coverage artifact
        uses: actions/download-artifact@v4
        with:
          name: analysis-coverage-${{ github.run_id }}
          path: analysis/build/reports/jacoco/test

      - name: Determine version for SonarCloud
        id: sonar-version
        run: |
          if [ "${{ github.ref_type }}" == "tag" ]; then
            echo "version=${{ github.ref_name }}" >> $GITHUB_OUTPUT
          else
            LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "0.0.0")
            echo "version=${LATEST_TAG}" >> $GITHUB_OUTPUT
          fi

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v5.0.0
        with:
          projectBaseDir: analysis
          args: >
            -Dsonar.projectVersion=${{ steps.sonar-version.outputs.version }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN_ANALYSIS }}
